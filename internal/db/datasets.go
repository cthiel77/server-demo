package db

// HeroSet a country dataset to be stored
type HeroSet struct {
	ID        uint64 `example:"123"`
	FirstName string `example:"Wade"`
	LastName  string `example:"Wilson"`
	HeroName  string `example:"Deadpool"`
}
